
Everything after `]` is interpreted as Mermaid syntax — and `🔍` is NOT valid.

---

# 🧪 Quick Test  
Paste this small block on GitHub to confirm everything works:

```md
```mermaid
flowchart TD
    A[Test] --> B[Success]
